services:
  quizbot:
    build: .
    image: tetraphobia/canvas-quizbot:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - quizbot-data:/data

volumes:
  quizbot-data:
